version: "2"

run:
  timeout: 10m
  tests: false

linters:
  enable:
    - ineffassign
    - unused
  disable:
    - errcheck
    - govet
    - staticcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
